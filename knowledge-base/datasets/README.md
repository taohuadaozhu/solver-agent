# Datasets

This folder stores dataset descriptions and metadata for the solver agent knowledge base.
Each dataset entry includes a `README.md` for human-readable description and a `metadata.json` for structured metadata.

These dataset documents are indexed by the RAG pipeline so that the agent can recommend appropriate benchmark instances and data sources.
